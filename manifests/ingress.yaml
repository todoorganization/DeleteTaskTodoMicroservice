apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-delete-tasks-api-ingress
spec:
  ingressClassName: azure-application-gateway
  rules:
    - host: delete-task-api.shambhushop.shop
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: todo-delete-tasks-api
                port:
                  number: 80
